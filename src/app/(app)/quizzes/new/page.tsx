import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function NewQuizPage() {
  return (
    <FeaturePlaceholder
      ctaHref="/profile"
      ctaLabel="Back to profile"
      description="Quiz builder flows arrive after the current auth and public exercise milestones. The protected route and navigation are already wired."
      stage="Stage 2-5 scaffold"
      title="Quiz builder shell"
    />
  );
}
